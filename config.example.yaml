# Equalify Iris deployment configuration (PRD §10.3).
#
# ${ENV_VAR} references are expanded from the process environment at startup.
# Changes require a restart in v1 (hot-reload is out of scope).

server:
  port: 8080
  # Public base URL of this deployment. Used to build the GitHub OAuth
  # callback URL and surfaced in device-flow instructions.
  base_url: http://localhost:8080
  # Shared secret gating GET /v1/quality, the deployment-wide tally of how good
  # the output has been (PRD §7.16). Read by the weekly quality-report workflow,
  # which files an issue when a rate crosses a threshold.
  #
  # Blank (default) means the endpoint answers 404 — opting in is deliberate.
  # Not the per-user GitHub auth the rest of the API uses: this is an aggregate
  # over every document the deployment has converted, so it belongs to no user,
  # and the caller is a CI job with no GitHub identity. Aggregate-only by
  # construction; no document content is exposed.
  quality_token: ${IRIS_QUALITY_TOKEN}

storage:
  # Root directory for sessions/, tmp/, and the agents/ git checkout.
  data_dir: ./data
  # Path to the agent library git checkout. Modified ONLY by `git pull`
  # from upstream (PRD §8.1).
  agents_dir: ./agents
  # Session metadata store. SQLite by default; a postgres URL is a supported
  # alternative for multi-instance deployments (not implemented in v1).
  database: ./data/iris.sqlite

# GitHub OAuth is the only auth mechanism (PRD §9.1), and a user's token is
# REQUIRED on every API call. That is the sustainability model, not a limitation:
# the token identifies the user AND files their session's feedback back to the
# shared agent library under their own identity, so using Iris and improving it are
# the same act (PRD §12). There is no anonymous mode and no way to consume the
# service without contributing.
#
# Nothing here opens pull requests: contributions are filed as issues (deviation
# from PRD §7.13 — see README "Implementation notes").
#
# The user's token is never persisted. It arrives in the `Authorization` header,
# stays in memory for the run it authorizes, and is gone when the run ends — so a
# copy of the SQLite database is not GitHub access.
github:
  # Leave blank to use the bundled Equalify Iris GITHUB APP via the device flow
  # (no per-operator app setup needed — like the GitHub CLI). Set it only to point
  # at your own GitHub App; its id starts `Iv`, and the id is not a secret.
  #
  # It must be a GitHub App, not an OAuth App: an `Ov…` id is refused at startup,
  # because Iris requests no OAuth scope and an OAuth App would therefore
  # authenticate users fine and then fail every issue filing. See the note below
  # `issue_token` for the registration settings your own app needs.
  #
  # Leaving this blank and changing `upstream_repo` is NOT enough on its own: the
  # bundled app is installed on EqualifyEverything/equalify-iris, and an app's
  # `issues: write` comes from its installation, so filing would fail for every user.
  # That combination warns at startup.
  client_id: ${GITHUB_CLIENT_ID}
  # Only required for the WEB redirect flow (/auth/github/start + /callback). The
  # device flow (CLI/Postman) needs no secret. Never commit a real secret.
  client_secret: ${GITHUB_CLIENT_SECRET}
  # The upstream agent library repo — where agent-suggestion issues are filed.
  # Changing this requires a GitHub App installed on THAT repo: see `client_id` above.
  upstream_repo: ${IRIS_UPSTREAM_REPO}
  # GitHub host base URLs. Override for GitHub Enterprise (e.g.
  # https://github.example.com/api/v3 and https://github.example.com). Defaults
  # to public GitHub when omitted.
  api_base_url: ${GITHUB_API_BASE_URL}
  oauth_base_url: ${GITHUB_OAUTH_BASE_URL}
  # OPTIONAL override for who authors auto-filed issues. Leave blank (the default):
  # issues are then filed as the logged-in user, which is the point of the design —
  # each contribution is credited to the person whose session produced it. A PAT here
  # files everything under one bot account instead and erases that attribution, so
  # set it only where a deployment genuinely cannot file as its users (e.g. an org
  # policy that forbids it).
  issue_token: ${IRIS_GITHUB_TOKEN}
  # No `oauth_scope` key: Iris authenticates as a GITHUB APP, which does not use
  # OAuth scopes. Its one permission — `issues: write` — comes from INSTALLING the app
  # on `upstream_repo`, so the consent screen users see requests no repository access
  # at all.
  #
  # If `upstream_repo` is PRIVATE, note that a user's token is limited to that user's
  # own access as well as the installation's: filing then works only for users who can
  # already see the repo, and 404s for the rest. Set `issue_token` above for a private
  # upstream that must accept contributions from non-collaborators.
  #
  # (An OAuth App could only express "file an issue" as `public_repo`: read AND write
  # to every public repo the user can reach, account-wide. That is what this replaced.)
  #
  # Pointing `client_id` at your own app therefore means registering a GitHub App with
  # device flow ENABLED and user-token expiry OFF, then installing it on your
  # `upstream_repo`. If filing logs a 403/404, the app is probably not installed — the
  # log line's `hint` says so.

# LLM provider abstraction (PRD §10.3). Each agent declares a capability;
# the deployment decides which provider serves it.
providers:
  default: openrouter
  # Per-agent overrides. Two forms:
  #   <agent>: <provider>                      # route this agent to a provider
  #   <agent>: { provider: ..., model: ... }   # also pin a specific model
  # Model resolution falls back: per-agent model -> provider per_capability ->
  # provider default_model. Agent names are the file stem. The agents actually
  # dispatched today are: page (extraction), reader + copy_editor (review),
  # feedback (verify/classify/train), builder (drafts suggested agents), plus any
  # specialist in agents/ that the page agent names.
  per_agent: {}
    # page: { model: anthropic/claude-sonnet-4.6 }   # cheaper/faster extraction
    # table: { model: anthropic/claude-opus-4.7 }    # stronger model, same provider
    # reader: { provider: bedrock, model: us.anthropic.claude-haiku-4-5-20251001-v1:0 }
    # Run the Feedback Agent (VERIFY/CLASSIFY/TRAIN) on a DIFFERENT or stronger
    # model than the page agent, so verification doesn't share the generator's
    # blind spots (recommended):
    # feedback: { model: anthropic/claude-opus-4.7 }

  # OpenRouter model IDs are `vendor/model` slugs (dot-versioned). Full list:
  # https://openrouter.ai/models
  openrouter:
    api_key: ${OPENROUTER_API_KEY}
    base_url: https://openrouter.ai/api/v1
    default_model: anthropic/claude-opus-4.7
    # Output-token ceiling per call. A dense page (a full-page table or form) is
    # the binding case; hit the ceiling and the model stops mid-tag. Iris now
    # FAILS such a call rather than passing the fragment downstream, so if you see
    # "hit the ... output ceiling", raise this. Omit for the default (32000).
    max_tokens: 32000
    # What one uploaded image may be, on this provider. LEAVE THIS UNSET — the
    # limits follow the model automatically (src/providers/imageLimits.ts knows the
    # documented per-image cap for each provider and the long edge for each Claude
    # generation), and they are what `POST /v1/sessions` rejects an oversized upload
    # by and what `GET /v1/limits` publishes.
    #
    # Override only for something that table cannot know: a model released after it
    # was written, or a platform with its own cap. Setting these too HIGH does not
    # raise what the model accepts — it re-creates the failure the check exists to
    # prevent, where an upload is accepted and then dies minutes later inside a model
    # call.
    # Each value is read from the block of the provider it belongs to and then the
    # STRICTEST across the agents that see an upload is what gets published, so a
    # tighter number set here still narrows the limit in a mixed deployment where
    # this provider only serves some of them.
    # image_limits:
    #   max_base64_bytes: 5242880   # provider's per-image cap, in base64 characters
    #   max_image_bytes: 3932160    # bytes on disk; defaults to 3/4 of the above
    #   max_long_edge_px: 1568      # published as advice; nothing is rejected for it
    per_capability:
      vision: anthropic/claude-opus-4.7
      structured_output: anthropic/claude-opus-4.7
      text: anthropic/claude-opus-4.7

  # Bedrock model IDs are hyphen-versioned and need a region prefix: current
  # Claude models are cross-region-inference only, so the bare `anthropic.*` form
  # is rejected for on-demand use — use `us.` (or `global.`). Confirm what your
  # account/region has enabled with:
  #   aws bedrock list-inference-profiles --region us-east-1 \
  #     --query 'inferenceProfileSummaries[].inferenceProfileId'
  bedrock:
    region: us-east-1
    default_model: us.anthropic.claude-opus-4-7
    # See the note on openrouter.max_tokens above. Retries are handled by the AWS
    # SDK's own `standard` strategy (3 attempts, exponential backoff, throttling +
    # 5xx + network errors), so there is no retry knob here.
    max_tokens: 32000
    # See the note on openrouter.image_limits above; the same override exists here
    # and is just as unnecessary in normal use.
    per_capability:
      vision: us.anthropic.claude-opus-4-7
      structured_output: us.anthropic.claude-opus-4-7
      text: us.anthropic.claude-opus-4-7

defaults:
  # How many reader/editor rounds a document gets before the review loop stops and
  # lists whatever is left in `unresolved.md`. This is the only place it is set:
  # it seeds each user's account default on first auth, and a session cannot
  # override it per request — how many rounds a document needs is discovered by
  # the loop, not something the uploader can know in advance.
  max_review_iterations: 3
  # How many pages to extract in parallel within a single run. Pages are
  # independent (one vision pass each), so this is a pure speed knob: a 25-page
  # document costs up to ~5 sequential model calls per page, and extracting
  # pages serially is the dominant source of end-to-end latency.
  #
  # Raise it if your provider's rate limits allow; lower it (or set 1 for fully
  # serial) if you hit 429s. Both adapters retry throttling responses (OpenRouter
  # by hand, Bedrock via the AWS SDK's standard strategy), but backing off is
  # slower than not being throttled. Clamped to 1..16; omit for the default.
  extraction_concurrency: 5
  # How many pipeline runs execute at once ACROSS sessions. This is the knob that
  # bounds what the machine as a whole is doing: each run holds a jsdom+axe
  # instance and up to extraction_concurrency model calls, so peak in-flight calls
  # are the PRODUCT of the two (the default 2 x 5 = 10).
  #
  # Uploads over the cap WAIT — a session sits in `status: "queued"` until a slot
  # frees, and the wait is recorded in its run log (`run_queued` / `run_dequeued`
  # with `waited_ms`). Nothing is rejected: the upload has already been received
  # and written to disk, so refusing it would throw away work the user has already
  # paid for. Order is strictly FIFO and the cap is global, not per user.
  #
  # This is the knob to raise first on a bigger box. Clamped to 1..32; omit for
  # the default. Needing more than a handful means you want multiple instances and
  # a shared Postgres store, which v1 does not implement.
  max_concurrent_runs: 2
